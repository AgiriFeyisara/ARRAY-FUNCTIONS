//Display a simple message outside a function that says, This is a text outside a function block.
let text='THis is a text outside a function block';
function outsideText(text){
        console.log(text)
       
    }
    outsideText(text);